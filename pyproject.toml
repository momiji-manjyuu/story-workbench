[project]
name = "story-workbench"
version = "0.1.0"
description = "File-first fiction worldbuilding and scene-state workbench."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
